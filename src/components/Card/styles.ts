import styled from 'styled-components';

export const Container = styled.div`
    background: white;
    padding: 1.5rem 2rem;
    border-radius: 0.25rem;
    margin-bottom: 2rem;
    color: var(--dark-blue);

    h3 {
        font-weight: normal;
        font-size: 1rem;
    }

    p {
        font-size: 2rem;
        line-height: 3rem;
        margin-top: 1rem;
    }

    .total {
        background: #49AA26;
        color: white;
    }
`;